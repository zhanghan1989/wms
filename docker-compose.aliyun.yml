services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    container_name: wms_api
    env_file:
      - apps/api/.env.aliyun
    ports:
      - "3000:3000"
    restart: unless-stopped

  # Optional reverse proxy (enable when domain + TLS are ready)
  # nginx:
  #   image: nginx:1.27-alpine
  #   container_name: wms_nginx
  #   depends_on:
  #     - api
  #   volumes:
  #     - ./deploy/nginx/wms.conf:/etc/nginx/conf.d/default.conf:ro
  #     - ./deploy/nginx/certs:/etc/nginx/certs:ro
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   restart: unless-stopped
