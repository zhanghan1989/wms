name: Deploy ECS

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      run_migrations:
        description: "Manual deploy: run prisma migrate deploy"
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"

concurrency:
  group: deploy-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Deploy to ECS by SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.ECS_HOST }}
          port: ${{ secrets.ECS_PORT }}
          username: ${{ secrets.ECS_USER }}
          key: ${{ secrets.ECS_SSH_KEY }}
          script_stop: true
          script: |
            set -eu
            retry() {
              max_attempts="$1"
              shift
              attempt=1
              while true; do
                if "$@"; then
                  return 0
                fi
                if [ "${attempt}" -ge "${max_attempts}" ]; then
                  echo "Command failed after ${attempt} attempts: $*"
                  return 1
                fi
                sleep_secs=$((attempt * 5))
                echo "Command failed (attempt ${attempt}/${max_attempts}). Retrying in ${sleep_secs}s: $*"
                sleep "${sleep_secs}"
                attempt=$((attempt + 1))
              done
            }
            cd ~/wms
            git fetch --all --prune
            git checkout main
            git reset --hard origin/main
            git rev-parse --short HEAD
            retry 3 docker compose up -d db redis
            echo "Running prisma migrate deploy..."
            retry 3 docker compose run --rm --build api npm run -w api prisma:migrate:deploy
            retry 3 docker compose up -d --build api
            docker compose ps
            docker compose logs --tail=120 api || true
            echo "Smoke check static assets..."
            READY=0
            for i in $(seq 1 30); do
              if curl -fsS http://127.0.0.1:3000/ >/dev/null 2>&1; then
                READY=1
                echo "API is ready (attempt ${i})."
                break
              fi
              sleep 2
            done
            if [ "${READY}" -ne 1 ]; then
              echo "API readiness check timeout."
              docker compose ps
              docker compose logs --tail=200 api || true
              exit 1
            fi
            curl -fI http://127.0.0.1:3000/
            curl -fI http://127.0.0.1:3000/app.js
            curl -fsS http://127.0.0.1:3000/app.js -o /tmp/wms_app.js
            grep -q "normalizeErrorMessage" /tmp/wms_app.js
            echo "Smoke check passed."
